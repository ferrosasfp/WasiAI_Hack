{
  "name": "marketplaceai-evm",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "hardhat compile",
    "clean": "hardhat clean",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network target",
    "coverage": "COVERAGE=1 hardhat coverage --testfiles test/Marketplace.spec.js",
    "coverage:check": "node scripts/check-coverage.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.13",
    "solidity-coverage": "^0.8.16"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  }
}
